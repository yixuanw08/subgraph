type Exchange @entity {
  # tx hash
  id: ID!

  # Block number
  block: BigInt!

  # Sender
  from: String!

  # Receiver
  to: String!

  # Amount of Tokens
  amount: BigInt!
}

# type User @entity {
#   id: ID!
#   name: String!
#   organizations: [UserOrganization!] @derivedFrom(field: "organization")
# }
